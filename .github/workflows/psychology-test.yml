name: 🧠 SOULCAKEY 심리테스트 자동 발행

on:
  workflow_dispatch:
    inputs:
      topic:
        description: '심리테스트 주제 (예: 연애유형, 음식, 직장스타일) — 빈칸이면 자동 검색'
        required: false
        default: '자동 검색'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 저장소 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 Python 설치
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 패키지 설치
        run: |
          pip install requests google-auth google-auth-oauthlib google-api-python-client

      - name: 🔍 테스트 생성 & 사이트 업데이트
        env:
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_SHEETS_ID: ${{ secrets.GOOGLE_SHEETS_ID }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          TOPIC: ${{ github.event.inputs.topic }}
        run: python scripts/generate.py

      - name: 📤 변경사항 커밋 & 푸시 (main)
        run: |
          git config --global user.name "SOULCAKEY Bot"
          git config --global user.email "bot@soulcakey.com"
          git add mindpop/tests/ mindpop/index.html
          git diff --staged --quiet || git commit -m "🧠 새 심리테스트 추가 $(date '+%Y-%m-%d')"
          git pull --rebase origin main
          git push

      - name: 🚀 gh-pages 브랜치로 배포
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./mindpop
